# Decision OS Staging Environment Variables
# Copy to .env.staging and fill in values (DO NOT commit secrets)

# PostgreSQL Database URL (Supabase, Neon, or other)
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://postgres:your-password@db.your-project.supabase.co:5432/postgres

# Environment (production for staging, not "staging")
NODE_ENV=production

# OCR Provider (disable for initial staging deployment)
OCR_PROVIDER=none

# Google Vision API Key (optional, only when OCR_PROVIDER=google_vision)
OCR_API_KEY=

# Optional: Custom OCR endpoint
# OCR_ENDPOINT=

# Staging API URL (for smoke tests)
STAGING_URL=https://your-app.vercel.app

# =============================================================================
# AUTHENTICATION (Supabase Auth)
# =============================================================================

# Supabase Project URL (from Project Settings → API)
# EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase Anon Key (from Project Settings → API)
# EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJ...

# Auth token for staging smoke tests (optional)
# Generate by logging in to your staging app and copying the access_token
# STAGING_AUTH_TOKEN=eyJ...

# Dev-only: Access token for testing (bypasses login UI)
# In dev mode, ApiService will attach this to Decision OS requests
# EXPO_PUBLIC_SUPABASE_ACCESS_TOKEN=eyJ...
